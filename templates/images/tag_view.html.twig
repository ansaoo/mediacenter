<ul class="tag-list" style="padding: 0">
    {% for val in agg %}
        {% if val %}
        <li>
            <a href="{{ path('images', {'tag':val.key}) }}"><i class="fa fa-tag"> {{val.key }} ({{ val.doc_count }})</i></a>
        </li>
        {% endif %}
    {% endfor %}
</ul>